openapi: 3.1.0
info:
  title: XTunnel Management API
  version: 0.1.0
  description: Management API contract for XTunnel Standalone V0.1.
servers:
  - url: /api/v1
    description: Same-origin XTunnel Server.

# M0 只建立可校验骨架。业务路径和 DTO 在 M5 契约冻结后加入。
paths: {}
