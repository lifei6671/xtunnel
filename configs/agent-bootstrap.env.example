# XTunnel Agent Bootstrap 环境模板
#
# 这不是 Agent 配置文件，Agent 不会自动读取本文件，也不支持 agent.yaml、
# --config、--set 或 --token-file。Service、Origin、Health Policy 和 Revision
# 只能由 Server 在认证后通过完整 Snapshot 下发，并仅在 Agent 内存中应用。
#
# 下面的值只是不可用的占位符。不要在仓库内保存或提交真实 Connection Token。
# Token 必须保持单个字符串的精确字节：不能有引号、首尾空白或换行，最大 8192 bytes。
# Token 泄露后应立即在管理端 Rotate 或 Revoke。

# 直接运行 `xtunnel-agent run` 或 `xtunnel-agent diagnose` 时使用这个变量。
# 来源优先级为：--token > XTUNNEL_TOKEN > OS Service Credential。
XTUNNEL_TOKEN=xta_REPLACE_WITH_TOKEN_FROM_WEB_CONSOLE

# Docker Compose 使用宿主变量 XTUNNEL_AGENT_TOKEN，并把它映射为容器内 XTUNNEL_TOKEN。
# 使用 Compose 时设置下面这个变量，不要同时复制上面的 XTUNNEL_TOKEN 到 Compose 文件。
# XTUNNEL_AGENT_TOKEN=xta_REPLACE_WITH_TOKEN_FROM_WEB_CONSOLE

# Linux systemd 生产部署不要 source 本文件。执行：
# sudo xtunnel-agent service install --token 'xta_...'
# 安装器会创建 root-only Credential Source，再由 systemd LoadCredential 注入。

# Windows SCM 生产部署也不要读取本文件。请在提升权限的 PowerShell 中执行：
# .\xtunnel-agent.exe service install --token 'xta_...'
# 安装器会使用 DPAPI Machine-scope 加密 Token，并设置受限 ACL。
